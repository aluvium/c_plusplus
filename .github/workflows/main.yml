name: Build
on:
  workflow_dispatch:
  push:
    branches:
     - master
  pull_request:
    branches:
      - master

jobs:
  release:    
    runs-on: 
     - ubuntu-latest
    steps:      
     - name: "Checkout Code"
       uses: actions/checkout@v3
     - name: "Build"
       uses: ./.github/actions/build/
